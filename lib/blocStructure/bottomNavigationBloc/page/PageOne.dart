import 'package:flutter/material.dart';

class FirstPage extends StatelessWidget {
  FirstPage() : super();

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('First page load'),
      ),
    );
  }
}
